name: Scope Legacy Rows

on:
  workflow_dispatch: {}

jobs:
  scope:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Run scope scan
        run: python3 scope_legacy_rows.py
